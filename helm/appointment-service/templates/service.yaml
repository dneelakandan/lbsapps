apiVersion: v1
kind: Service
metadata:
  name: appointment-service
spec:
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 3001
  type: {{ .Values.service.type }}
